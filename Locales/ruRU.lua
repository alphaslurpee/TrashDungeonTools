if not(GetLocale() == "ruRU") then
  return
end
local addonName, TDT = ...
local L = TDT.L
L = L or {}

--@localization(locale="ruRU", format="lua_additive_table", namespace="TDT", handle-subnamespaces="none")@