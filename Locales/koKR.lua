if not(GetLocale() == "koKR") then
  return
end
local addonName, TDT = ...
local L = TDT.L
L = L or {}

--@localization(locale="koKR", format="lua_additive_table", namespace="TDT", handle-subnamespaces="none")@